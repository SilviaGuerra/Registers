import { css } from 'glamor';

const styles = css({
    display: 'flex',
    flexDirection: 'column',
    ' .data': {
        '&__card': {
            border: '1px solid',
            margin: '15px 0 10px'
        }
    }
});

export default styles;